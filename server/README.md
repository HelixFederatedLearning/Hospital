# Doctor Pro Portal — Server

## Setup
1) Copy `.env.example` to `.env` and fill:
```
MONGODB_URI=...
JWT_SECRET=...
PORT=4000
CLIENT_ORIGIN=http://localhost:5173
```
2) Install and run:
```
npm install
npm run dev
```
